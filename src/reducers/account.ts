import {  TypeBaseAction } from "../interface/action"

const init = {
  isFetching: false,
  isReady: false,
  auth: false
}

const account = (state =init, action:TypeBaseAction ) =>{
  return state
}

export default account